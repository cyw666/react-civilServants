import config from './config'
import fetch from './request'
import {
  getCookie,
  setCookie,
  delCookie,
  setStore,
  getStore,
  removeStore,
  dateFilter,
  wordLimit,
  JudgeStatus,
  delHtmlTag,
  queryURL,
  examAllScore,
  countIf,
  rightScore,
  formatTime,
  fullScreen,
  exitFullScreen,
} from './utils'


export {
  config,
  fetch,
  getCookie,
  setCookie,
  delCookie,
  setStore,
  getStore,
  removeStore,
  dateFilter,
  wordLimit,
  JudgeStatus,
  delHtmlTag,
  queryURL,
  examAllScore,
  countIf,
  rightScore,
  formatTime,
  fullScreen,
  exitFullScreen,
}
